{
  "name": "bidstation-functions",
  "description": "Firebase Cloud Functions for BIDSTATION (notifications, autobid, analytics, security)",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "echo \"No build step (plain JS).\"",
    "lint": "eslint . || echo \"No lint configured\"",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
